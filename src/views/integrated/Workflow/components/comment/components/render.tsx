import type { WorkflowNodeEntity } from '@flowgram.ai/free-layout-editor';
/**
 * 注释渲染组件
 * @param node 节点
 * @returns 返回注释渲染组件
 */
export const CommentRender = ({ node }: { node: WorkflowNodeEntity }) => {
  return <div>注释渲染组件</div>;
};
