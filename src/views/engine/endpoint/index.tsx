import React from 'react';

/**
 * 端点管理（Engine - Endpoint）
 * 占位组件，后续实现端点与端点类型配置的 CRUD。
 */
const EngineEndpoint: React.FC = () => {
  return (
    <div>
      <h1>端点管理（Engine - Endpoint）</h1>
    </div>
  );
};

export default EngineEndpoint;
