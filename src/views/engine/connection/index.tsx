import React from 'react';

/**
 * 连接/数据库（Engine - Connection）
 * 占位组件，后续实现引擎侧数据库连接管理。
 */
const EngineConnection: React.FC = () => {
  return (
    <div>
      <h1>连接/数据库（Engine - Connection）</h1>
    </div>
  );
};

export default EngineConnection;
